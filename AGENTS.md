# 프로젝트 목표와 협업 원칙

이 문서는 세션과 worktree가 바뀌어도 유지할 작업 원칙이다. 일시적인 진행 상황은 `docs/codex-dev-log/`에 기록하고, 실제 구현 상태는 현재 코드를 확인한다. 사용자의 최신 명시적 지시가 이 문서보다 우선한다.

## 프로젝트 목표

- TypeScript로 자체 에이전트 하네스를 만들며 구조와 핵심 구현을 이해한다. 완성된 코드를 받는 것뿐 아니라 사용자가 설계 이유와 실행 흐름을 설명할 수 있게 되는 것이 중요하다.
- 기본 학습 범위는 시스템 프롬프트, 세션과 저장/resume, 툴 호출, 스킬, 모델 요청에 들어가는 컨텍스트 조립·관리다.
- DSH(DeepSeek Harness)의 구조와 설계 판단을 이해하고, 필요한 개념을 우리 구현에 대응시킨다.
- 확장 과제는 Bakery Codex Farm의 GPT-5.6 Luna를 연결한 하네스로 완성도 높은 테트리스를 제작하는 것이다. Anthropic Messages와 Claude Haiku를 사용하는 구성으로도 전환할 수 있어야 한다.
- Luna 연결은 Responses API를 목표로 한다. 실제 Farm의 주소·인증·모델 ID·지원 기능은 제공된 가이드로 확인하고 추측하지 않는다.
- 컨텍스트 압축, 툴 호출 보호, 캐싱, 긴 툴 결과 줄이기를 실제 작업 품질과 연결해 이해한다. 그다음 과제는 하네스가 만든 테트리스를 직접 플레이하는 것이다.
- 기본 대화 API 호출이 모델을 재학습시키는 것으로 설명하지 않는다. 세션에 저장한 기록과 이번 요청에 조립해 넣는 컨텍스트를 구분한다.

## 작업 진행 방식

- 기본 순서는 **방향과 설계 이유 설명 → 핵심 구현 코드 설명 → 사용자 이해·확인 → 에이전트가 구현·검증**이다.
- 사용자가 질문하거나 설계를 논의할 때 곧바로 코드를 수정하지 않는다. “구현해줘”, “고쳐줘”처럼 실행을 요청하면 합의한 범위에서 구현한다.
- 초반의 “사용자가 모든 코드를 직접 작성한다”는 방식은 변경되었다. 지금은 설명과 확인 후 에이전트가 코딩한다.
- 한 번에 다루는 개념과 변경 범위를 작게 유지한다. 큰 과제는 실행하고 확인할 수 있는 단계로 나눈다.
- 학습 루프는 **최소 구현 → 이해 → 실제 사용 → 문제 발견 → 실제 하네스의 해결책이 필요한 이유 이해 → 다음 최소 구현**이다.
- 속도와 구현 완성도도 고려하되, 사용자의 이해 단계를 건너뛰거나 동의하지 않은 기능까지 확대하지 않는다.

## 설명 방식

- 기본 설명은 한국어로 한다. 비유를 사용하지 않는다.
- 추상적 정의보다 작은 TypeScript 코드와 실제 값을 먼저 보여준다.
- 어느 코드가 실행되고, 어떤 값이 만들어지며, 누가 어떤 함수를 호출하는지 순서대로 설명한다.
- 클래스·인스턴스·플러그인·서비스와 설정 개념을 한꺼번에 섞어 설명하지 않는다. “A는 B다”라고 말할 때 어떤 관점의 분류인지 분명히 한다.
- 변수와 객체 참조 등을 학습에 필요 이상으로 엄밀하게 구분하지 않는다. 새로운 용어를 한 번에 많이 도입하지 않는다.
- 사용자가 특정 줄을 질문하면 그 줄과 이해에 필요한 주변 코드부터 설명한다. 다음 설계 전체를 반복하지 않는다.
- 간단한 문법 예시, 실제 소스, 설계 초안을 명확히 구분한다. 확인하지 않은 코드를 본 것처럼 말하지 않는다.

## 설계와 구현 기준

- 처음부터 DSH 규모의 확장성이나 추상화를 만들지 않는다. 실제 문제가 생겼거나 이번 과제에 필요한 구조만 도입한다.
- 단순한 모듈 분리와 플러그인 구조 도입은 다르다. 파일 분리만 필요할 때 서비스 컨테이너나 플러그인 시스템까지 도입하지 않는다.
- Cordis를 사용하는 DSH의 방향은 장기적인 학습 대상이지만, 다중 API 지원 등 다른 기능의 필수 선행 조건으로 취급하지 않는다.
- DSH 등의 구현은 실제 소스를 확인해 참고하고, 필요한 설계만 축소해서 채택한다. 라이브러리나 대규모 구조를 이유 없이 통째로 가져오지 않는다.
- “최소 구현”을 유지한다. 다만 사용자가 채택한 툴 검증·오류 피드백·컨텍스트 관리까지 불필요한 기능으로 취급하거나 제거하지 않는다.
- 모델이 도구 선택, 단계 반복, 중간 보고를 판단하도록 하는 실험 의도를 보존한다. 원하는 결과가 안 나온다고 특정 툴 순서나 작업별 절차를 코드에 강제하지 않는다.
- 카운터 증가와 값 조회를 따로 둔 것은 모델이 번갈아 호출하도록 실험하기 위한 의도적인 선택이다. 임의로 합치지 않는다.
- 개발 중에는 하위 호환을 기본 요구사항으로 삼지 않는다. 구형 세션 자동 변환이나 fallback을 임의로 추가하지 말고, 형식 변경의 영향을 알려준다.
- 하위 호환을 생략하는 것과 기존 데이터 삭제는 다르다. 사용자 요청 없이 세션 파일 등을 삭제하지 않는다.

## 주석과 문서

- 새 코드뿐 아니라 기존 코드도 함수·타입·인터페이스·클래스·메서드 바로 위에 역할을 설명하는 짧은 한국어 주석을 한 줄 둔다.
- 별도 역할을 가진 이름 있는 화살표 함수와 테스트 보조 함수도 같은 기준을 적용한다. 단순 map/filter 콜백마다 설명을 반복하지는 않는다.
- 주석은 문법을 그대로 읽는 대신 무엇을 받거나 만들고 무엇을 담당하는지 설명한다. 코드가 바뀌면 주석도 함께 맞춘다.
- 사용자가 명시적으로 요청하지 않는 한 README를 작성·수정하지 않는다.
- 구현 결정, 변경 내역, 검증 결과를 기록할 필요가 있으면 `docs/codex-dev-log/`를 사용한다.
- 키·토큰이나 실제 비밀 값을 코드, 문서, 테스트, 로그에 넣지 않는다.

## Git과 병렬 작업

- **커밋·병합·동기화는 사용자가 직접 한다.** 에이전트는 임의로 commit, merge, rebase, cherry-pick, push 등의 작업을 하지 않는다.
- 현재 디렉토리와 git 상태를 확인하고 현재 worktree에서 작업한다. 메인 checkout을 임의로 수정하지 않는다.
- git log/status/diff 등 읽기 전용 확인과 사용자가 수행할 명령 설명은 가능하다.
- 다른 세션이 맡은 범위와 변경이 겹치는지 확인한다. 겹치는 부분은 먼저 알리고 조율하며, 기존 미커밋 변경을 덮어쓰지 않는다.
- 완료 보고에서는 변경 내용과 검증 결과를 알려주고 커밋·병합 판단은 사용자에게 남긴다.

## 확인과 검증

- 현재 코드 상태를 기억에 의존하지 말고 직접 읽는다. API 형식과 제공자 기능은 실제 가이드와 공식 문서로 확인한다.
- API 프로토콜, 모델, 연결 서버를 구분한다. 호환 API라는 이유만으로 모든 기능이 같거나 모든 모델을 쓸 수 있다고 가정하지 않는다.
- 런타임·실행 방법·패키지 명령은 현재 설정을 확인한다. `.ts` 직접 실행과 빌드된 `.js` 실행을 혼동하지 않는다.
- 변경 위험에 맞게 테스트와 타입 검사·빌드를 수행하고, 기존 기능이 유지되는지 확인한다.
- 모의 응답 테스트와 실제 API 호출 검증을 구분해서 보고한다. 테스트 통과만으로 실제 모델 연결까지 확인했다고 말하지 않는다.
- 순수 주석 변경은 실행 코드가 그대로인지 확인한다. 설명용 변경에 기능 수정이나 불필요한 리팩터링을 섞지 않는다.
